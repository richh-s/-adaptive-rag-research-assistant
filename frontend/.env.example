# Base URL of the FastAPI backend (rag-assistant serve). Defaults to http://127.0.0.1:8000
# if unset -- only needed if the backend runs on a different host/port.
VITE_API_BASE_URL=http://127.0.0.1:8000
