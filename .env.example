# Google AI Studio API key (free tier, no credit card required)
# Required always -- Gemini handles embeddings regardless of which chat provider is primary,
# and serves as the chat fallback when ANTHROPIC_API_KEY is set.
# Get one at https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# Tavily API key (free tier)
# Get one at https://app.tavily.com (sign up / dashboard, not the bare tavily.com homepage)
TAVILY_API_KEY=

# Anthropic API key (optional). When set, Claude becomes the primary chat/reasoning model
# for routing, decomposition, grading, and synthesis, with automatic fallback to Gemini on
# error. Leave blank to use Gemini for everything, as before.
# Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Optional overrides — defaults shown
# GEMINI_CHAT_MODEL=gemini-2.5-flash
# GEMINI_EMBEDDING_MODEL=models/gemini-embedding-001
# ANTHROPIC_CHAT_MODEL=claude-sonnet-5
